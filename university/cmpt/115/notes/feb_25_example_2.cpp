#include <iostream>
#include <cmath>

using namespace std;
//testing

void foo(Node test,) {
	//if head == NULL
	// ERROR

	//(*head).value + foo((*head).next)
	//and return that
}

int main() {

	cout << foo(?) << endl;
	cout << "Works"
	return 0;
}